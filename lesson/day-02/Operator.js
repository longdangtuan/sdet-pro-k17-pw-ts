let myNumber1 = 1;
let myNumber2 = 2;

// 1. Right operand and left operand
let result = myNumber1 + myNumber2;

// String litteral
console.log(`Total value: ${result}`);

// Rounding nearest
let roundingNearest = Math.round(9 / 2);
console.log(roundingNearest);

// Rounding floor
let roundingFloor = Math.floor(9 / 2);
console.log(roundingFloor);

// Rounding ceiling
let roundingCeiling = Math.ceil(11 / 2);
console.log(roundingCeiling);

// To fix number
let anotherResult = 10 / 3;
let toFixedNumber = anotherResult.toFixed(2);
console.log(toFixedNumber);
